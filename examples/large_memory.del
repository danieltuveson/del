function main() {
    for let i = 0; i < 1000000; i++ {
        let str = "this will leak memory, but should get freed when the VM ";
        let str2 = "is done running this program."::Array<byte>;
    }
}
