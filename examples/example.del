// dim thing as int, y as int
// 
// y = 1
// thing = 9 - 4 + 4 / 2  - 1
// print("hello, w")
//
// Some function

class Stuff {
    property: int;
    otherprop: int;
    // function dostuff(thing: int) {
    //     inner = thing(4);
    // }
}

// function Stuff(a: int, b: int, c: int, d: int) {
//     return a - b;
// }

// Entrypoint
function main(thing: int) {
    s = Stuff(111, 222);
}
