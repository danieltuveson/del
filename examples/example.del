// function addFive(arg: int, argg: int): int {
//     return arg - argg;
// }
// 
// function addFive(arg: int, argg: int): int {
//     return arg + argg;
// }

class Thing {
    stuff: int;
    more: int;
    // function Thing(stuff: int) {
    //     this.stuff = stuff;
    //     return;
    // }
}

class ThingContainer {
    thing: Thing;
}

// Entrypoint
function main() {
    let t = new Thing(123, 456);
    let x = new Thing(101, 202);
    let x = new Thing(303, 404);
}

